/** @jsx React.DOM */

define([
  'react',
], function(React) {
  var Footer = React.createClass({
    render: function() {
      return (
        <footer>
        </footer>
      );
    }
  });

  return Footer;
});
