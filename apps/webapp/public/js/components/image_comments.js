/** @jsx React.DOM */

var React = require('react');

var ImageComments = React.createClass({
  render: function() {
    return (
      <h3>Some comments..</h3>
    );
  }
});

module.exports = ImageComments;
